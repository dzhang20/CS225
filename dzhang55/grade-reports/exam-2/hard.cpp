#include "linkedlist.h"

void llUnion(LinkedList* lst1,
           LinkedList* lst2,
           LinkedList* unn)
{
	LinkedList unn;
	int max = 0;
	if(lst1==lst2){
		while(lst1->head->next!=NULL)
			for
	while((*lst1).head!=NULL&&(*lst2)!=NULL){
	}	
	return;
}
